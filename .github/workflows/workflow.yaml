name: Android E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e-tests:
    runs-on: ubuntu-latest

    env:
      ANDROID_HOME: /opt/android-sdk

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Instalar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Instalar dependências do sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y wget unzip libvirt-daemon-system qemu-kvm

      - name: Instalar Android SDK (Command Line Tools)
        run: |
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
          unzip cmdline-tools.zip
          mv cmdline-tools latest
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "emulator" "platforms;android-33" "build-tools;33.0.2" "system-images;android-33;google_apis;x86_64"

      - name: Adicionar Android SDK ao PATH
        run: |
          echo "$ANDROID_HOME/platform-tools" >> $GITHUB_PATH
          echo "$ANDROID_HOME/emulator" >> $GITHUB_PATH
          echo "$ANDROID_HOME/cmdline-tools/latest/bin" >> $GITHUB_PATH

      - name: Criar e iniciar o emulador Android
        run: |
          $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager create avd -n testEmu -k "system-images;android-33;google_apis;x86_64" --device "pixel" --force
          $ANDROID_HOME/emulator/emulator -list-avds
          nohup $ANDROID_HOME/emulator/emulator -avd testEmu -no-window -no-audio -no-boot-anim -gpu swiftshader_indirect &
          sleep 30
          adb wait-for-device
          adb shell input keyevent 82

      - name: Instalar Node.js e dependências do projeto
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Instalar Appium e dependências do projeto
        run: |
          npm install -g appium
          appium driver install uiautomator2
          npm install

      - name: Iniciar Appium
        run: |
          nohup appium --log-level error &
          sleep 10

      - name: Rodar os testes automatizados
        run: npx wdio run wdio.conf.js
